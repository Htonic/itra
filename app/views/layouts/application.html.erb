<!DOCTYPE html>
<html>
  <head>
    <title>Ourcrowd</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag    "https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>

  </head>
  <header>
    <nav class="navbar navbar-expand-lg nav-ligth bg-">
      <a class="navbar-brand" href="#">OURcrowd</a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
          <li class="nav-item active">
            <a class="nav-link" href=/><%=t 'link.home' %><span class="sr-only">(current)</span></a>
          </li>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              <%= t 'language' %>
            </a>
            <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                <%= link_to 'English',set_language_en_path,class: "dropdown-item"%>
              <%= link_to 'Русский',set_language_ru_path,class: "dropdown-item"%>
            </div>
          </li>
          <% if user_signed_in? %>
          <li class="nav-item">
            <a class="nav-link" href="/users/:user_id"> <%= t('link.my_account')%></a>
          </li>
        </ul>
          <%end %>
        <form class="form-inline my-2 my-lg-0">
          <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
          <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
        </form>
      </div>
      <% if user_signed_in? %>
      <%= link_to t('link.logout'),  destroy_user_session_path %>
        <%end %>
    </nav>
  </header>
  <body>
 
  <p class="notice"><%= notice %></p>
<p class="alert"><%= alert %></p>
    <%= yield %>
  </body>
</html>
